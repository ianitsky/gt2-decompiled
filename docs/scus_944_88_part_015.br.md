# Documentação do Arquivo scus_944.88_part_015.c

## Visão Geral

Este documento descreve todas as funções presentes no arquivo `scus_944.88_part_015.c`, que contém código decompilado do Gran Turismo 2 para PlayStation 1. Este arquivo contém uma única função muito complexa relacionada ao rendering 3D usando GTE (Geometry Transformation Engine) do PlayStation, com suporte a múltiplos tipos de polígonos e subdivisão recursiva.

## Índice

1. [Módulo de Rendering 3D com Subdivisão Recursiva (Overlay0)](#módulo-de-rendering-3d-com-subdivisão-recursiva-overlay0)

---

## Módulo de Rendering 3D com Subdivisão Recursiva (Overlay0)

Este módulo contém uma função extremamente complexa que renderiza polígonos 3D usando GTE com suporte a múltiplos tipos de polígonos e subdivisão recursiva para melhor qualidade visual.

### FUN_overlay0__8001c17c

**Linhas:** 2-2866  
**Tipo:** `void FUN_overlay0__8001c17c(SVECTOR **param_1, uint param_2, uint param_3)`

**Descrição:**  
Função extremamente complexa de rendering 3D que processa múltiplos tipos de polígonos usando GTE (Geometry Transformation Engine) do PlayStation. Esta função implementa rendering com subdivisão recursiva adaptativa para melhor qualidade visual, processando até 9 tipos diferentes de polígonos simultaneamente.

**Parâmetros:**
- `param_1`: Ponteiro para array de ponteiros SVECTOR contendo:
  - `param_1[0]`: Ponteiro para array de vértices transformados (buffer de saída)
  - `param_1[1]`: Ponteiro para dados de polígonos tipo 1 (triângulos simples)
  - `param_1[2]`: Ponteiro para dados de polígonos tipo 2 (quads simples)
  - `param_1[3]`: Ponteiro para dados de polígonos tipo 3 (triângulos com gradiente de cor)
  - `param_1[4]`: Ponteiro para dados de polígonos tipo 4 (quads com gradiente de cor)
  - `param_1[5]`: Ponteiro para dados de polígonos tipo 5 (triângulos com subdivisão recursiva)
  - `param_1[6]`: Ponteiro para dados de polígonos tipo 6 (quads com subdivisão recursiva)
  - `param_1[7]`: Ponteiro para dados de polígonos tipo 7 (quads com textura e gradiente)
  - `param_1[8]`: Ponteiro para dados de polígonos tipo 8 (quads complexos com múltiplas cores)
  - Campos adicionais contendo contadores e configurações
- `param_2`: Parâmetro de configuração adicional
- `param_3`: Parâmetro de configuração adicional

**Dependências:**
- `gte_ldv0`, `gte_ldVXY0`, `gte_ldVZ0`, `gte_ldVXY1`, `gte_ldVZ1`, `gte_ldVXY2`, `gte_ldVZ2`: Carrega vértices no GTE
- `gte_rtps_b`: Transforma ponto único usando GTE
- `gte_rtpt_b`: Transforma triângulo usando GTE
- `gte_nclip_b`: Calcula normal do polígono para culling
- `gte_dpcs`, `gte_dpcs_b`: Aplica correção de perspectiva de cor
- `gte_dpct`, `gte_dpct_b`: Aplica correção de perspectiva de cor com interpolação
- `gte_stRGB2`, `gte_stRGB0`, `gte_stRGB1`: Armazena cores calculadas
- `gte_stSXY0`, `gte_stSXY1`, `gte_stSXY2`: Armazena coordenadas de tela
- `gte_stFLAG`: Obtém flags de resultado da transformação
- `gte_stMAC0`: Obtém resultado de cálculo matemático
- `gte_stIR1`, `gte_stIR2`, `gte_stIR3`: Obtém valores intermediários
- `gte_stSZ3`: Obtém valor de profundidade
- `gte_ldRGB`, `gte_ldRGB0`, `gte_ldRGB1`, `gte_ldRGB2`: Carrega cores no GTE
- `gte_ldIR0`: Carrega valor intermediário
- `gte_ldSXY0`, `gte_ldSXY1`, `gte_ldSXY2`: Carrega coordenadas de tela
- `gte_ldSXYP`: Carrega coordenada de tela adicional
- `gte_ldR11R12`, `gte_ldR13R21`, `gte_ldR22R23`, `gte_ldR31R32`, `gte_ldR33`: Carrega matriz de rotação
- `gte_ldtr`: Carrega vetor de translação
- `gte_op0_b`: Operação matemática específica
- `gte_mvmva_b`: Multiplicação matriz-vetor
- `gte_llir_b`: Operação de interpolação linear
- `gte_lcv1_b`: Operação de conversão
- `gte_ldLZCS`: Carrega valor para cálculo de leading zeros
- `gte_stLZCR`: Obtém contagem de leading zeros
- `read_sz_fifo3`: Lê valores de profundidade do FIFO
- `read_sxsy_fifo3`: Lê coordenadas de tela do FIFO
- `read_mt`: Lê valores de matriz
- `FUN_80081580`: Processa polígonos com clipping adicional
- `FUN_8007bcd0`: Processa polígonos com subdivisão recursiva

**Notas:**
- Esta é uma das funções mais complexas do jogo, com mais de 2800 linhas de código
- Processa até 9 tipos diferentes de polígonos simultaneamente
- Implementa subdivisão recursiva adaptativa baseada em distância e tamanho do polígono
- Usa GTE extensivamente para todas as transformações 3D
- Aplica clipping e culling de polígonos otimizados
- Gerencia diferentes modos de rendering (cor sólida, gradiente, textura, etc.)
- Implementa correção de perspectiva para cores e coordenadas de textura
- Usa buffers de trabalho na memória scratchpad (0x1f800000)
- Processa vértices em lotes para otimização
- Implementa subdivisão recursiva para polígonos grandes ou distantes
- Gerencia múltiplos níveis de detalhe (LOD) baseados em distância
- Aplica transformações de matriz para polígonos com back-face culling
- Processa diferentes tipos de primitivas GPU (G3, GT3, G4, GT4, etc.)

**Fluxo de Processamento:**

1. **Fase 1 - Transformação de Vértices Iniciais:**
   - Transforma vértices usando `gte_rtps_b`
   - Calcula flags de visibilidade e clipping
   - Armazena resultados em buffer de saída

2. **Fase 2 - Processamento de Polígonos Tipo 1 (Triângulos Simples):**
   - Processa triângulos com cor sólida
   - Aplica culling usando `gte_nclip_b`
   - Calcula correção de perspectiva usando `gte_dpcs`
   - Cria primitivas GPU tipo G3

3. **Fase 3 - Processamento de Polígonos Tipo 2 (Quads Simples):**
   - Processa quads com cor sólida
   - Divide quads em triângulos quando necessário
   - Aplica clipping adicional
   - Cria primitivas GPU tipo G4

4. **Fase 4 - Processamento de Polígonos Tipo 3 (Triângulos com Gradiente):**
   - Processa triângulos com gradiente de cor
   - Interpola cores entre vértices usando `gte_dpct`
   - Cria primitivas GPU tipo GT3

5. **Fase 5 - Processamento de Polígonos Tipo 4 (Quads com Gradiente):**
   - Processa quads com gradiente de cor
   - Interpola cores entre 4 vértices
   - Cria primitivas GPU tipo GT4

6. **Fase 6 - Processamento de Polígonos Tipo 5 (Triângulos com Subdivisão):**
   - Processa triângulos que requerem subdivisão recursiva
   - Divide triângulos em 4 sub-triângulos quando necessário
   - Aplica subdivisão adaptativa baseada em distância
   - Usa `FUN_8007bcd0` para processamento recursivo

7. **Fase 7 - Processamento de Polígonos Tipo 6 (Quads com Subdivisão):**
   - Processa quads que requerem subdivisão recursiva
   - Divide quads em múltiplos triângulos quando necessário
   - Aplica subdivisão adaptativa baseada em distância
   - Usa `FUN_8007bcd0` para processamento recursivo

8. **Fase 8 - Processamento de Polígonos Tipo 7 (Quads com Textura e Gradiente):**
   - Processa quads com textura e gradiente de cor
   - Interpola coordenadas de textura e cores
   - Cria primitivas GPU tipo GT4 com textura

9. **Fase 9 - Processamento de Polígonos Tipo 8 (Quads Complexos):**
   - Processa quads com múltiplas cores e texturas
   - Aplica transformações de matriz complexas
   - Implementa subdivisão recursiva para quads grandes
   - Cria primitivas GPU tipo GT4 complexas

**Otimizações Implementadas:**

- **Culling Otimizado:** Usa `gte_nclip_b` para culling rápido de polígonos back-facing
- **Clipping Eficiente:** Aplica clipping apenas quando necessário
- **Subdivisão Adaptativa:** Subdivide polígonos apenas quando necessário baseado em distância e tamanho
- **Processamento em Lotes:** Processa múltiplos polígonos em sequência para melhor uso do pipeline GTE
- **Uso de Scratchpad:** Usa memória scratchpad (0x1f800000) para buffers temporários rápidos
- **Cálculo de LOD:** Calcula nível de detalhe baseado em distância para otimizar rendering
- **Interpolação Otimizada:** Usa GTE para interpolação de cores e coordenadas de textura

**Estruturas de Dados:**

- Buffer de vértices transformados: Armazena vértices transformados com flags de visibilidade
- Buffer de primitivas GPU: Armazena comandos GPU para rendering
- Estruturas de polígonos: Contêm índices de vértices, cores, e flags
- Buffers temporários: Usados para subdivisão recursiva e cálculos intermediários

**Casos Especiais:**

- **Back-face Culling:** Detecta e descarta polígonos que não estão visíveis
- **Clipping de Bordas:** Clippa polígonos que cruzam bordas da tela
- **Subdivisão Recursiva:** Divide polígonos grandes em polígonos menores para melhor qualidade
- **Correção de Perspectiva:** Aplica correção de perspectiva para cores e texturas
- **Transformações de Matriz:** Aplica transformações de matriz para polígonos com orientação específica

---

## Referências Cruzadas

### Relação com Outras Funções

Esta função é chamada por funções de rendering de nível superior e trabalha em conjunto com:

- `FUN_overlay0__80019b58`: Função similar de rendering com subdivisão recursiva
- `FUN_overlay0__80018d1c`: Função de rendering básico de polígonos
- `FUN_overlay0__800196bc`: Função de clipping de polígonos
- `FUN_80081580`: Função de processamento de clipping adicional
- `FUN_8007bcd0`: Função de subdivisão recursiva de polígonos

### Fluxo de Rendering

1. Transformação de vértices → 2. Culling → 3. Clipping → 4. Correção de perspectiva → 5. Criação de primitivas GPU → 6. Subdivisão (se necessário) → 7. Renderização final

---

## Notas Finais

Esta função representa o coração do sistema de rendering 3D do Gran Turismo 2. Sua complexidade reflete a necessidade de renderizar gráficos de alta qualidade em hardware limitado do PlayStation 1. A implementação faz uso extensivo do GTE para cálculos de transformação 3D otimizados e implementa técnicas avançadas como subdivisão recursiva adaptativa para melhor qualidade visual.

A função processa múltiplos tipos de polígonos simultaneamente, aplicando diferentes técnicas de rendering baseadas no tipo de polígono e nas condições de visibilidade. A implementação é altamente otimizada para o hardware do PlayStation 1, usando buffers de scratchpad e processamento em lotes para maximizar o desempenho.
