# Documentation for File scus_944.88_part_015.c

## Overview

This document describes all functions present in the file `scus_944.88_part_015.c`, which contains decompiled code from Gran Turismo 2 for PlayStation 1. This file contains a single very complex function related to 3D rendering using the PlayStation's GTE (Geometry Transformation Engine), with support for multiple polygon types and recursive subdivision.

## Table of Contents

1. [3D Rendering with Recursive Subdivision Module (Overlay0)](#3d-rendering-with-recursive-subdivision-module-overlay0)

---

## 3D Rendering with Recursive Subdivision Module (Overlay0)

This module contains an extremely complex function that renders 3D polygons using GTE with support for multiple polygon types and recursive subdivision for better visual quality.

### FUN_overlay0__8001c17c

**Lines:** 2-2866  
**Type:** `void FUN_overlay0__8001c17c(SVECTOR **param_1, uint param_2, uint param_3)`

**Description:**  
Extremely complex 3D rendering function that processes multiple polygon types using the PlayStation's GTE (Geometry Transformation Engine). This function implements rendering with adaptive recursive subdivision for better visual quality, processing up to 9 different polygon types simultaneously.

**Parameters:**
- `param_1`: Pointer to array of SVECTOR pointers containing:
  - `param_1[0]`: Pointer to array of transformed vertices (output buffer)
  - `param_1[1]`: Pointer to polygon type 1 data (simple triangles)
  - `param_1[2]`: Pointer to polygon type 2 data (simple quads)
  - `param_1[3]`: Pointer to polygon type 3 data (triangles with color gradient)
  - `param_1[4]`: Pointer to polygon type 4 data (quads with color gradient)
  - `param_1[5]`: Pointer to polygon type 5 data (triangles with recursive subdivision)
  - `param_1[6]`: Pointer to polygon type 6 data (quads with recursive subdivision)
  - `param_1[7]`: Pointer to polygon type 7 data (quads with texture and gradient)
  - `param_1[8]`: Pointer to polygon type 8 data (complex quads with multiple colors)
  - Additional fields containing counters and configurations
- `param_2`: Additional configuration parameter
- `param_3`: Additional configuration parameter

**Dependencies:**
- `gte_ldv0`, `gte_ldVXY0`, `gte_ldVZ0`, `gte_ldVXY1`, `gte_ldVZ1`, `gte_ldVXY2`, `gte_ldVZ2`: Loads vertices into GTE
- `gte_rtps_b`: Transforms single point using GTE
- `gte_rtpt_b`: Transforms triangle using GTE
- `gte_nclip_b`: Calculates polygon normal for culling
- `gte_dpcs`, `gte_dpcs_b`: Applies color perspective correction
- `gte_dpct`, `gte_dpct_b`: Applies color perspective correction with interpolation
- `gte_stRGB2`, `gte_stRGB0`, `gte_stRGB1`: Stores calculated colors
- `gte_stSXY0`, `gte_stSXY1`, `gte_stSXY2`: Stores screen coordinates
- `gte_stFLAG`: Gets transformation result flags
- `gte_stMAC0`: Gets mathematical calculation result
- `gte_stIR1`, `gte_stIR2`, `gte_stIR3`: Gets intermediate values
- `gte_stSZ3`: Gets depth value
- `gte_ldRGB`, `gte_ldRGB0`, `gte_ldRGB1`, `gte_ldRGB2`: Loads colors into GTE
- `gte_ldIR0`: Loads intermediate value
- `gte_ldSXY0`, `gte_ldSXY1`, `gte_ldSXY2`: Loads screen coordinates
- `gte_ldSXYP`: Loads additional screen coordinate
- `gte_ldR11R12`, `gte_ldR13R21`, `gte_ldR22R23`, `gte_ldR31R32`, `gte_ldR33`: Loads rotation matrix
- `gte_ldtr`: Loads translation vector
- `gte_op0_b`: Specific mathematical operation
- `gte_mvmva_b`: Matrix-vector multiplication
- `gte_llir_b`: Linear interpolation operation
- `gte_lcv1_b`: Conversion operation
- `gte_ldLZCS`: Loads value for leading zero calculation
- `gte_stLZCR`: Gets leading zero count
- `read_sz_fifo3`: Reads depth values from FIFO
- `read_sxsy_fifo3`: Reads screen coordinates from FIFO
- `read_mt`: Reads matrix values
- `FUN_80081580`: Processes polygons with additional clipping
- `FUN_8007bcd0`: Processes polygons with recursive subdivision

**Notes:**
- This is one of the most complex functions in the game, with over 2800 lines of code
- Processes up to 9 different polygon types simultaneously
- Implements adaptive recursive subdivision based on polygon distance and size
- Uses GTE extensively for all 3D transformations
- Applies optimized polygon clipping and culling
- Manages different rendering modes (solid color, gradient, texture, etc.)
- Implements perspective correction for colors and texture coordinates
- Uses work buffers in scratchpad memory (0x1f800000)
- Processes vertices in batches for optimization
- Implements recursive subdivision for large or distant polygons
- Manages multiple levels of detail (LOD) based on distance
- Applies matrix transformations for polygons with back-face culling
- Processes different GPU primitive types (G3, GT3, G4, GT4, etc.)

**Processing Flow:**

1. **Phase 1 - Initial Vertex Transformation:**
   - Transforms vertices using `gte_rtps_b`
   - Calculates visibility and clipping flags
   - Stores results in output buffer

2. **Phase 2 - Polygon Type 1 Processing (Simple Triangles):**
   - Processes triangles with solid color
   - Applies culling using `gte_nclip_b`
   - Calculates perspective correction using `gte_dpcs`
   - Creates G3 type GPU primitives

3. **Phase 3 - Polygon Type 2 Processing (Simple Quads):**
   - Processes quads with solid color
   - Divides quads into triangles when necessary
   - Applies additional clipping
   - Creates G4 type GPU primitives

4. **Phase 4 - Polygon Type 3 Processing (Triangles with Gradient):**
   - Processes triangles with color gradient
   - Interpolates colors between vertices using `gte_dpct`
   - Creates GT3 type GPU primitives

5. **Phase 5 - Polygon Type 4 Processing (Quads with Gradient):**
   - Processes quads with color gradient
   - Interpolates colors between 4 vertices
   - Creates GT4 type GPU primitives

6. **Phase 6 - Polygon Type 5 Processing (Triangles with Subdivision):**
   - Processes triangles that require recursive subdivision
   - Divides triangles into 4 sub-triangles when necessary
   - Applies adaptive subdivision based on distance
   - Uses `FUN_8007bcd0` for recursive processing

7. **Phase 7 - Polygon Type 6 Processing (Quads with Subdivision):**
   - Processes quads that require recursive subdivision
   - Divides quads into multiple triangles when necessary
   - Applies adaptive subdivision based on distance
   - Uses `FUN_8007bcd0` for recursive processing

8. **Phase 8 - Polygon Type 7 Processing (Quads with Texture and Gradient):**
   - Processes quads with texture and color gradient
   - Interpolates texture coordinates and colors
   - Creates GT4 type GPU primitives with texture

9. **Phase 9 - Polygon Type 8 Processing (Complex Quads):**
   - Processes quads with multiple colors and textures
   - Applies complex matrix transformations
   - Implements recursive subdivision for large quads
   - Creates complex GT4 type GPU primitives

**Implemented Optimizations:**

- **Optimized Culling:** Uses `gte_nclip_b` for fast back-facing polygon culling
- **Efficient Clipping:** Applies clipping only when necessary
- **Adaptive Subdivision:** Subdivides polygons only when necessary based on distance and size
- **Batch Processing:** Processes multiple polygons in sequence for better GTE pipeline usage
- **Scratchpad Usage:** Uses scratchpad memory (0x1f800000) for fast temporary buffers
- **LOD Calculation:** Calculates level of detail based on distance to optimize rendering
- **Optimized Interpolation:** Uses GTE for color and texture coordinate interpolation

**Data Structures:**

- Transformed vertex buffer: Stores transformed vertices with visibility flags
- GPU primitive buffer: Stores GPU commands for rendering
- Polygon structures: Contain vertex indices, colors, and flags
- Temporary buffers: Used for recursive subdivision and intermediate calculations

**Special Cases:**

- **Back-face Culling:** Detects and discards polygons that are not visible
- **Edge Clipping:** Clips polygons that cross screen edges
- **Recursive Subdivision:** Divides large polygons into smaller polygons for better quality
- **Perspective Correction:** Applies perspective correction for colors and textures
- **Matrix Transformations:** Applies matrix transformations for polygons with specific orientation

---

## Cross References

### Relationship with Other Functions

This function is called by higher-level rendering functions and works together with:

- `FUN_overlay0__80019b58`: Similar rendering function with recursive subdivision
- `FUN_overlay0__80018d1c`: Basic polygon rendering function
- `FUN_overlay0__800196bc`: Polygon clipping function
- `FUN_80081580`: Additional clipping processing function
- `FUN_8007bcd0`: Recursive polygon subdivision function

### Rendering Flow

1. Vertex transformation → 2. Culling → 3. Clipping → 4. Perspective correction → 5. GPU primitive creation → 6. Subdivision (if necessary) → 7. Final rendering

---

## Final Notes

This function represents the heart of Gran Turismo 2's 3D rendering system. Its complexity reflects the need to render high-quality graphics on the PlayStation 1's limited hardware. The implementation makes extensive use of GTE for optimized 3D transformation calculations and implements advanced techniques such as adaptive recursive subdivision for better visual quality.

The function processes multiple polygon types simultaneously, applying different rendering techniques based on polygon type and visibility conditions. The implementation is highly optimized for PlayStation 1 hardware, using scratchpad buffers and batch processing to maximize performance.
