# Documentation for File scus_944.88_part_018.c

## Overview

This document describes all functions present in the file `scus_944.88_part_018.c`, which contains decompiled code from Gran Turismo 2 for PlayStation 1. This file contains functions related to overlay0, including 3D transformations using GTE, pointer relocation in data structures, overlay data loading and processing, collision/polygon detection system, and car data processing.

## Table of Contents

1. [3D Transformations and Geometry Module (Overlay0)](#3d-transformations-and-geometry-module-overlay0)
2. [Pointer Relocation Module (Overlay0)](#pointer-relocation-module-overlay0)
3. [Data Loading and Processing Module (Overlay0)](#data-loading-and-processing-module-overlay0)
4. [Collision and Detection System Module (Overlay0)](#collision-and-detection-system-module-overlay0)
5. [Car Data Processing Module (Overlay0)](#car-data-processing-module-overlay0)

---

## 3D Transformations and Geometry Module (Overlay0)

This module contains functions related to 3D transformations using GTE, geometry calculations, and coordinate processing.

### FUN_overlay0__80026bb4

**Lines:** 2-84  
**Type:** `void FUN_overlay0__80026bb4(int param_1, int param_2)`

**Description:**  
Calculates and applies 3D transformations using GTE based on relative position between two objects. The function calculates distance between objects, determines appropriate level of detail (LOD), generates rotation matrix using auxiliary function, and applies rotation and translation transformations using GTE.

**Parameters:**
- `param_1`: Pointer to structure containing origin object position and orientation
- `param_2`: Pointer to rendering context structure where results are stored

**Features:**
- Calculates quadratic distance between objects using 3D coordinates
- Determines level of detail (LOD) based on distance (0, 1 or 2)
- Generates rotation matrix using auxiliary function
- Applies rotation transformations using GTE (R11-R33)
- Calculates and applies translation using GTE
- Stores results in context structure

**Dependencies:**
- `FUN_80081a34`: Generates rotation matrix based on parameters
- `read_mt`: Reads transformation matrix data
- GTE functions: `gte_ldR11R12`, `gte_ldR13R21`, `gte_ldR22R23`, `gte_ldR31R32`, `gte_ldR33`, `gte_ldsv_`, `gte_rtir_b`, `gte_ldtr`

**Notes:**
- Uses approximate square root calculation to determine LOD
- Normalizes coordinates before applying transformations
- Stores calculated translation in `param_2 + 0x78`, `0x7c`, `0x80`

---

### FUN_overlay0__80027c70

**Lines:** 749-855  
**Type:** `int FUN_overlay0__80027c70(int param_1, uint *param_2, int *param_3)`

**Description:**  
Calculates Z coordinate (depth) of a 3D point projected onto a polygon using barycentric interpolation. The function supports triangles and quadrilaterals, calculates barycentric coordinates, and interpolates Z coordinate based on point position on polygon.

**Parameters:**
- `param_1`: Pointer to structure containing polygon and vertex data
- `param_2`: Pointer to polygon data (contains vertex indices and flags)
- `param_3`: Pointer to 3D coordinates of point to be projected

**Return:**
- Integer value representing interpolated Z coordinate

**Features:**
- Supports triangles and quadrilaterals (flag `0x40000000`)
- For quadrilaterals: calculates center and determines vertex order based on relative position
- Calculates barycentric coordinates using cross product
- Interpolates Z coordinate using barycentric coordinates
- Uses fixed-point division for precise calculations

**Dependencies:**
- `FUN_80086084`: High precision fixed-point division

**Notes:**
- Uses barycentric interpolation for precise depth calculation
- Supports convex and concave polygons
- Returns Z coordinate in fixed-point format

---

### FUN_overlay0__80027fc4

**Lines:** 857-988  
**Type:** `void FUN_overlay0__80027fc4(int param_1, int *param_2)`

**Description:**  
Processes multiple polygons and calculates collision/detection information using GTE transformations. The function processes up to 4 polygons, applies rotation and lighting transformations, calculates screen coordinates, and determines collision information based on cross product calculations.

**Parameters:**
- `param_1`: Pointer to structure containing polygon data and transformations
- `param_2`: Pointer to structure containing vertex data and results

**Features:**
- Processes up to 4 polygons simultaneously
- Calculates screen coordinates (X, Y) for each vertex
- Applies rotation transformations using GTE matrices (R11-R33)
- Applies lighting transformations using GTE matrices (L11-L33)
- Calculates collision information using cross product
- Updates minimum depth data when necessary

**Dependencies:**
- GTE functions for transformations (`gte_ldR11R12`, `gte_ldR13R21`, `gte_ldR22R23`, `gte_ldR31R32`, `gte_ldR33`)
- GTE functions for lighting (`gte_ldL11L12`, `gte_ldL13L21`, `gte_ldL22L23`, `gte_ldL31L32`, `gte_ldL33`)
- GTE functions for matrix multiplication (`gte_mvmva_b`)
- `FUN_80086084`: Fixed-point division

**Notes:**
- Processes polygons in iterative loop
- Uses cross product calculations to determine orientation
- Updates minimum depth when polygon is closer

---

## Pointer Relocation Module (Overlay0)

This module contains functions that adjust pointers in data structures after loading or memory relocation.

### FUN_overlay0__80026db0

**Lines:** 86-99  
**Type:** `void FUN_overlay0__80026db0(int *param_1, int param_2)`

**Description:**  
Adjusts pointers in a data structure by adding an offset. Iterates over 11 elements of the structure (including the first element) and adds `param_2` to each pointer.

**Parameters:**
- `param_1`: Pointer to structure containing 11 pointers
- `param_2`: Offset to be added to each pointer

**Features:**
- Adjusts pointers in data structure
- Used to relocate rendering structures in memory

**Notes:**
- Processes elements from index 0 to 10
- Used during data loading/relocation

---

### FUN_overlay0__80026e0c

**Lines:** 106-144  
**Type:** `void FUN_overlay0__80026e0c(int param_1, int param_2)`

**Description:**  
Adjusts pointers in a hierarchical data structure. Processes up to 16 entries, each containing a base pointer and a list of secondary pointers. Adds `param_2` to all pointers in the hierarchy.

**Parameters:**
- `param_1`: Pointer to hierarchical structure containing up to 16 entries
- `param_2`: Offset to be added to each pointer

**Features:**
- Processes hierarchical pointer structure
- Adjusts base pointer of each entry
- Adjusts secondary pointers in linked list
- Used to relocate complex structures in memory

**Notes:**
- Processes up to 16 main entries
- Each entry can have multiple secondary pointers
- Used during overlay data loading

---

### FUN_overlay0__80026e88

**Lines:** 151-176  
**Type:** `void FUN_overlay0__80026e88(int param_1, int param_2)`

**Description:**  
Adjusts pointers in a complex data structure by calling multiple relocation functions. This function coordinates relocation of different parts of the structure, including pointers to matrices, lists, and other nested structures.

**Parameters:**
- `param_1`: Pointer to data structure to be relocated
- `param_2`: Offset to be added to pointers

**Features:**
- Adjusts pointer at offset `0x94`
- Calls relocation function for structure at `0x94`
- Adjusts and processes pointer at offset `0x98`
- Adjusts and processes pointer at offset `0x9c`
- Adjusts and processes pointer at offset `0xa0`
- Adjusts pointers in structure at offset `0xa4`

**Dependencies:**
- `FUN_overlay0__80026db0`: Relocates simple structure
- `FUN_overlay0__80026e04`: Stub function (does nothing)
- `FUN_overlay0__80026e0c`: Relocates hierarchical structure
- `FUN_overlay0__80026e80`: Stub function (does nothing)

**Notes:**
- Coordinates multiple relocation operations
- Used during overlay data loading

---

### FUN_overlay0__80026f10

**Lines:** 178-212  
**Type:** `void FUN_overlay0__80026f10(int param_1, int param_2)`

**Description:**  
Adjusts pointers in a data structure containing a list of nested structures. The function adjusts base pointer, adjusts pointers in the list, and updates cross-references between structures before calling recursive relocation function.

**Parameters:**
- `param_1`: Pointer to structure containing list of nested structures
- `param_2`: Offset to be added to pointers

**Features:**
- Adjusts base pointer at offset `0x8`
- Adjusts pointers in list (number specified at offset `0x4`)
- Updates cross-references between structures
- Calls recursive relocation function for each structure in list

**Dependencies:**
- `FUN_overlay0__80026e88`: Relocates complex structure

**Notes:**
- Processes dynamic list of structures
- Updates cross-references before relocation
- Used for structures with multiple nesting levels

---

### FUN_overlay0__80027010

**Lines:** 219-230  
**Type:** `void FUN_overlay0__80027010(uint *param_1, int param_2)`

**Description:**  
Adjusts pointers in a data structure containing an array of pointers. The function adjusts pointers at even positions of the array (indices 2, 4, 6, etc.) by adding `param_2` to each.

**Parameters:**
- `param_1`: Pointer to structure containing counter and array of pointers
  - `param_1[0]`: Number of pointers in array
  - `param_1[2]`, `param_1[4]`, ...: Pointers to be adjusted
- `param_2`: Offset to be added to pointers

**Features:**
- Calls auxiliary function before processing
- Iterates over pointer array
- Adjusts pointers at even positions

**Dependencies:**
- `FUN_8007adc8`: Auxiliary function (probably initialization)

**Notes:**
- Processes variable-size array
- Used for structures with pointer arrays

---

### FUN_overlay0__80027074

**Lines:** 232-250  
**Type:** `void FUN_overlay0__80027074(uint *param_1, int param_2)`

**Description:**  
Adjusts pointers in a hierarchical structure containing an array of structures, each containing its own pointers. The function adjusts main pointers and calls recursive function to adjust secondary pointers.

**Parameters:**
- `param_1`: Pointer to structure containing counter and array of structures
  - `param_1[0]`: Number of structures in array
  - `param_1[1]`, `param_1[2]`, ...: Pointers to secondary structures
- `param_2`: Offset to be added to pointers

**Features:**
- Adjusts main pointers in array
- Calls recursive function for each secondary structure
- Processes hierarchy of nested structures

**Dependencies:**
- `FUN_overlay0__80027010`: Adjusts pointers in secondary structure

**Notes:**
- Processes multi-level hierarchical structure
- Used for structures with cross-references

---

### FUN_overlay0__800270fc

**Lines:** 252-270  
**Type:** `void FUN_overlay0__800270fc(uint *param_1, int param_2)`

**Description:**  
Similar to `FUN_overlay0__80027074`, but calls different function for secondary structure relocation. Adjusts main pointers and delegates secondary structure relocation to specific function.

**Parameters:**
- `param_1`: Pointer to structure containing counter and array of structures
- `param_2`: Offset to be added to pointers

**Features:**
- Adjusts main pointers in array
- Calls specific function for each secondary structure
- Processes structure hierarchy

**Dependencies:**
- `FUN_overlay0__800200bc`: Adjusts pointers in secondary structure (function from another file)

**Notes:**
- Similar to `FUN_overlay0__80027074` but uses different function
- Used for specific types of structures

---

### FUN_overlay0__80027184

**Lines:** 272-281  
**Type:** `void FUN_overlay0__80027184(ushort *param_1, int param_2)`

**Description:**  
Adjusts pointers in a structure containing an array of pointers stored as integers. The function adjusts pointers at specific positions of the array (indices 2, 4, 6, etc.) by adding `param_2` to each.

**Parameters:**
- `param_1`: Pointer to structure containing counter and array of pointers
  - `param_1[0]`: Number of pointers in array (ushort)
  - `param_1[2]`, `param_1[4]`, ...: Pointers to be adjusted (stored as int)
- `param_2`: Offset to be added to pointers

**Features:**
- Iterates over pointer array
- Adjusts pointers at specific positions
- Used for structures with pointer arrays

**Notes:**
- Processes variable-size array
- Pointers stored as 32-bit integers

---

### FUN_overlay0__800271c0

**Lines:** 283-322  
**Type:** `void FUN_overlay0__800271c0(int param_1)`

**Description:**  
Main relocation function that adjusts all pointers in a complex data structure. This function coordinates relocation of multiple sub-structures, including matrices, lists, and nested structures, calling specific functions for each type.

**Parameters:**
- `param_1`: Pointer to main data structure to be relocated (also used as base offset)

**Features:**
- Adjusts pointers at offsets `0x10`, `0x14`, `0x18`, `0x1c`
- Adjusts array of 33 pointers at offset `0x118`
- Adjusts pointer at offset `0x20`
- Calls specific relocation functions for each sub-structure:
  - `FUN_overlay0__80026f10` for structure at `0x10`
  - `FUN_overlay0__80027074` for structure at `0x14`
  - `FUN_overlay0__800270fc` for structure at `0x18`
  - `FUN_overlay0__80027008` for each entry at `0x118` (33 times)
  - `FUN_overlay0__80027184` for structure at `0x1c`
  - `FUN_overlay0__80038da0` for structure at `0x20`

**Dependencies:**
- `FUN_overlay0__80026f10`: Relocates structure with nested list
- `FUN_overlay0__80027074`: Relocates hierarchical structure
- `FUN_overlay0__800270fc`: Relocates alternative hierarchical structure
- `FUN_overlay0__80027008`: Stub function (does nothing)
- `FUN_overlay0__80027184`: Adjusts pointer array
- `FUN_overlay0__80038da0`: External relocation function

**Notes:**
- Main relocation function for complex structures
- Coordinates multiple relocation operations
- Used during overlay data loading

---

## Data Loading and Processing Module (Overlay0)

This module contains functions related to overlay data loading, processing, and initialization.

### FUN_overlay0__80026e04

**Lines:** 101-104  
**Type:** `void FUN_overlay0__80026e04(void)`

**Description:**  
Stub function that does nothing. Returns immediately without executing any operation.

**Notes:**
- Placeholder function or unimplemented function
- Can be used as empty callback

---

### FUN_overlay0__80026e80

**Lines:** 146-149  
**Type:** `void FUN_overlay0__80026e80(void)`

**Description:**  
Stub function that does nothing. Returns immediately without executing any operation.

**Notes:**
- Placeholder function or unimplemented function
- Can be used as empty callback

---

### FUN_overlay0__80027008

**Lines:** 214-217  
**Type:** `void FUN_overlay0__80027008(void)`

**Description:**  
Stub function that does nothing. Returns immediately without executing any operation.

**Notes:**
- Placeholder function or unimplemented function
- Can be used as empty callback

---

### FUN_overlay0__800272c8

**Lines:** 324-339  
**Type:** `void FUN_overlay0__800272c8(int *param_1)`

**Description:**  
Processes a list of data structures, calling processing function for each entry. The function iterates over the list, processes each entry using auxiliary function, and advances to next entry based on returned size.

**Parameters:**
- `param_1`: Pointer to structure containing counter and data list
  - `param_1[0]`: Number of entries in list
  - `param_1[1]`, ...: Data of each entry

**Features:**
- Iterates over structure list
- Calls processing function for each entry
- Aligns pointer to next entry based on returned size

**Dependencies:**
- `FUN_8007bca0`: Processes individual entry and returns processed size

**Notes:**
- Processes variable-size list
- Used to process overlay data after loading

---

### FUN_overlay0__8002732c

**Lines:** 341-349  
**Type:** `void FUN_overlay0__8002732c(int param_1)`

**Description:**  
Gets car data identifier and stores it in main structure. The function searches car data using global identifier and stores the result.

**Parameters:**
- `param_1`: Pointer to main structure where result will be stored

**Features:**
- Searches car data using global identifier
- Stores result at offset `0x5d30`

**Dependencies:**
- `FUN_80060eb4`: Searches car data by identifier
- `DAT_801d589c`: Global car identifier

**Notes:**
- Used to initialize car data
- Stores result as byte

---

### FUN_overlay0__8002736c

**Lines:** 351-363  
**Type:** `void FUN_overlay0__8002736c(int param_1, int param_2)`

**Description:**  
Loads and processes overlay data based on index. The function loads data from two different tables, processes loaded data, and stores results in different locations of main structure.

**Parameters:**
- `param_1`: Pointer to main structure
- `param_2`: Index used to search data in tables

**Features:**
- Searches data in table `DAT_801e2f7c` using index
- Loads data to temporary buffer at offset `0xe5700`
- Processes loaded data
- Loads additional data to offset `0x5d34`

**Dependencies:**
- `FUN_8005d92c`: Loads data from table
- `FUN_overlay0__800272c8`: Processes loaded data

**Notes:**
- Uses index to search in data table
- Processes data in temporary buffer before storing

---

### FUN_overlay0__800273d8

**Lines:** 365-378  
**Type:** `void FUN_overlay0__800273d8(int param_1, undefined4 param_2)`

**Description:**  
Loads and processes overlay data based on identifier. The function searches data using identifier, loads related data, and processes using callback function.

**Parameters:**
- `param_1`: Pointer to main structure
- `param_2`: Identifier used to search data

**Features:**
- Searches data using identifier
- Loads related data from table
- Processes data using callback function
- Stores data in temporary buffer

**Dependencies:**
- `FUN_80060e94`: Searches data by identifier
- `FUN_80060fb0`: Gets pointer to related data
- `FUN_8005d92c`: Loads data
- `FUN_8007bca0`: Processes data with callback

**Notes:**
- Uses identifier instead of index
- Processes data with specific callback function

---

### FUN_overlay0__8002743c

**Lines:** 380-404  
**Type:** `void FUN_overlay0__8002743c(int param_1, int param_2)`

**Description:**  
Loads and initializes complete overlay data for a specific car. This function coordinates loading of multiple data types, processes complex structures, and initializes rendering system for the car.

**Parameters:**
- `param_1`: Pointer to main structure
- `param_2`: Car identifier

**Features:**
- Loads initial data using identifier
- Loads table data for structure at offset `0xb534`
- Processes loaded data
- Relocates data structures
- Loads additional data based on car flags

**Dependencies:**
- `FUN_overlay0__800273d8`: Loads initial data
- `FUN_80060e94`: Searches car data
- `FUN_8005d92c`: Loads table data
- `FUN_overlay0__800272c8`: Processes data
- `FUN_overlay0__800271c0`: Relocates structures
- `FUN_overlay0__8002736c`: Loads additional data

**Notes:**
- Main initialization function for car data
- Coordinates multiple loading operations
- Used during overlay data loading

---

### FUN_overlay0__800274d4

**Lines:** 406-456  
**Type:** `void FUN_overlay0__800274d4(int param_1)`

**Description:**  
Configures and initializes rendering system for car data. The function loads rendering data, configures screen parameters, and prepares structures for rendering car-specific elements.

**Parameters:**
- `param_1`: Pointer to main structure

**Features:**
- Loads rendering data (type `0x6d`)
- Configures screen parameters (resolution, position)
- Processes rendering data
- Configures additional elements based on car flags
- Prepares structures for rendering

**Dependencies:**
- `FUN_8005d8a0`: Loads rendering data
- `FUN_overlay0__8002eb08`: Processes rendering data
- `FUN_80060e94`: Searches car data
- `FUN_8007bca0`: Processes rendering structures

**Notes:**
- Configures rendering system for specific car
- Uses car flags to determine settings
- Prepares multiple rendering elements

---

### FUN_overlay0__800275e8

**Lines:** 458-601  
**Type:** `void FUN_overlay0__800275e8(int param_1, undefined4 param_2)`

**Description:**  
Processes and renders interface/text elements related to car data. The function searches text data, processes multiple interface elements, applies random transformations for visual variation, and configures rendering elements.

**Parameters:**
- `param_1`: Pointer to main structure
- `param_2`: Car identifier

**Features:**
- Checks car flags to determine if processing is necessary
- Searches text data filename
- Loads and processes text data
- Applies random transformations for interface elements
- Configures coordinates and rendering parameters
- Processes multiple interface elements in loop

**Dependencies:**
- `FUN_80060e94`: Searches car data
- `FUN_8005d8d4`: Loads text data
- `FUN_8008cf00`: Searches entry in text data
- `FUN_8007d23c`: Generates random number
- `FUN_8008ce30`: Initializes array
- `FUN_80083ae0`: Generates additional random number
- `FUN_8007bca0`: Processes rendering structures

**Notes:**
- Processes interface/text elements for car
- Uses random numbers for visual variation
- Configures multiple rendering elements

---

### FUN_overlay0__8002797c

**Lines:** 603-617  
**Type:** `void FUN_overlay0__8002797c(int param_1)`

**Description:**  
Initializes complete car data system if not yet initialized. The function checks initialization flag and, if necessary, calls sequence of functions to load and configure all car data.

**Parameters:**
- `param_1`: Pointer to main structure

**Features:**
- Checks initialization flag at offset `0x11d700`
- If not initialized, executes initialization sequence:
  1. Loads car overlay data
  2. Configures rendering system
  3. Processes interface/text elements
- Marks system as initialized

**Dependencies:**
- `FUN_overlay0__8002743c`: Loads overlay data
- `FUN_overlay0__800274d4`: Configures rendering system
- `FUN_overlay0__800275e8`: Processes interface elements

**Notes:**
- Main initialization function for car data
- Ensures single initialization using flag
- Coordinates multiple initialization operations

---

## Collision and Detection System Module (Overlay0)

This module contains functions related to collision detection, polygon selection, and intersection calculations.

### FUN_overlay0__800279e8

**Lines:** 619-720  
**Type:** `uint * FUN_overlay0__800279e8(short *param_1, int param_2, uint param_3, int param_4)`

**Description:**  
Searches for polygon containing a specific point using spatial grid system. The function converts point coordinates to grid coordinates, searches polygons in corresponding cell, and tests each polygon using GTE clipping to determine which contains the point.

**Parameters:**
- `param_1`: Pointer to spatial grid structure containing cell information
- `param_2`: Pointer to vertex data
- `param_3`: X coordinate of point
- `param_4`: Y coordinate of point

**Return:**
- Pointer to polygon containing the point, or `NULL` if no polygon contains the point

**Features:**
- Converts point coordinates to grid coordinates
- Searches polygons in corresponding cell
- Tests each polygon using GTE clipping (`gte_nclip_b`)
- Determines which polygon contains the point based on clipping results
- Returns first polygon that contains the point

**Dependencies:**
- GTE functions for clipping (`gte_ldSXY0`, `gte_ldSXY1`, `gte_ldSXY2`, `gte_nclip_b`, `gte_stMAC0`)

**Notes:**
- Uses spatial grid system for optimization
- Tests polygons using GTE clipping for precision
- Returns `NULL` if point is outside all polygons

---

### FUN_overlay0__80027bbc

**Lines:** 722-736  
**Type:** `uint FUN_overlay0__80027bbc(int param_1, int *param_2)`

**Description:**  
Checks if a 3D point is inside a volume (bounding box or frustum). The function calculates relative position of point, applies transformation, and returns flag indicating if point is inside volume.

**Parameters:**
- `param_1`: Pointer to structure containing volume definition
- `param_2`: Pointer to 3D coordinates of point

**Return:**
- `1` if point is inside volume, `0` otherwise

**Features:**
- Calculates relative position of point relative to volume
- Applies transformation using auxiliary function
- Determines if point is inside volume
- Returns boolean flag

**Dependencies:**
- `FUN_80081a78`: Applies transformation and checks if point is inside

**Notes:**
- Used for frustum culling or bounding box checking
- Returns simple boolean flag

---

### FUN_overlay0__80027c1c

**Lines:** 738-747  
**Type:** `void FUN_overlay0__80027c1c(int param_1, int *param_2)`

**Description:**  
Searches for polygon containing a 3D point projected on screen. The function converts 3D coordinates to grid coordinates, and calls polygon search function.

**Parameters:**
- `param_1`: Pointer to rendering context structure
- `param_2`: Pointer to 3D coordinates of point

**Features:**
- Converts 3D coordinates to grid coordinates
- Normalizes coordinates using bit masks
- Calls polygon search function

**Dependencies:**
- `FUN_overlay0__800279e8`: Searches for polygon containing point

**Notes:**
- Converts 3D coordinates to 2D before searching polygon
- Uses coordinate normalization for precision

---

## Car Data Processing Module (Overlay0)

This module contains functions related to specific car data processing and related calculations.

### (No additional functions in this module)

**Notes:**  
Car data processing functions are integrated in previous modules, especially in the data loading and processing module.

---

## Function Summary by Module

### 3D Transformations and Geometry Module (Overlay0)
- `FUN_overlay0__80026bb4`: Calculates and applies 3D transformations using GTE
- `FUN_overlay0__80027c70`: Calculates Z coordinate using barycentric interpolation
- `FUN_overlay0__80027fc4`: Processes polygons and calculates collision information

### Pointer Relocation Module (Overlay0)
- `FUN_overlay0__80026db0`: Adjusts pointers in simple structure
- `FUN_overlay0__80026e0c`: Adjusts pointers in hierarchical structure
- `FUN_overlay0__80026e88`: Coordinates relocation of complex structure
- `FUN_overlay0__80026f10`: Adjusts pointers in structure with nested list
- `FUN_overlay0__80027010`: Adjusts pointers in pointer array
- `FUN_overlay0__80027074`: Adjusts pointers in recursive hierarchical structure
- `FUN_overlay0__800270fc`: Adjusts pointers in alternative hierarchical structure
- `FUN_overlay0__80027184`: Adjusts pointers in pointer array (alternative format)
- `FUN_overlay0__800271c0`: Main relocation function for complex structure

### Data Loading and Processing Module (Overlay0)
- `FUN_overlay0__80026e04`: Stub function (does nothing)
- `FUN_overlay0__80026e80`: Stub function (does nothing)
- `FUN_overlay0__80027008`: Stub function (does nothing)
- `FUN_overlay0__800272c8`: Processes list of data structures
- `FUN_overlay0__8002732c`: Gets car data identifier
- `FUN_overlay0__8002736c`: Loads overlay data by index
- `FUN_overlay0__800273d8`: Loads overlay data by identifier
- `FUN_overlay0__8002743c`: Loads and initializes complete overlay data
- `FUN_overlay0__800274d4`: Configures rendering system for car
- `FUN_overlay0__800275e8`: Processes car interface/text elements
- `FUN_overlay0__8002797c`: Initializes complete car data system

### Collision and Detection System Module (Overlay0)
- `FUN_overlay0__800279e8`: Searches for polygon containing point using spatial grid
- `FUN_overlay0__80027bbc`: Checks if point is inside volume
- `FUN_overlay0__80027c1c`: Searches for polygon containing projected 3D point

**Total documented functions:** 25

---

## Technical Observations

### Pointer Relocation System
The file contains a complete pointer relocation system used during overlay data loading. The system supports:
- Simple structures with pointer arrays
- Hierarchical structures with multiple levels
- Cross-references between structures
- Linked lists of structures

### Spatial Grid System
Function `FUN_overlay0__800279e8` implements a spatial grid system to optimize polygon search:
- Divides space into grid cells
- Stores polygons by cell
- Converts coordinates to grid indices
- Tests only polygons in relevant cell

### Car Data Processing
The car data processing system includes:
- Overlay data loading
- Structure relocation in memory
- Rendering system configuration
- Interface element processing
- Single initialization using flags

### GTE Usage
Several functions make extensive use of GTE for:
- 3D transformations (rotation, translation)
- Clipping and culling calculations
- Coordinate interpolation
- 3D geometry calculations
