# Documentation for File scus_944.88_part_003.c

## Overview

This document describes all functions present in the file `scus_944.88_part_003.c`, which contains decompiled code from Gran Turismo 2 for PlayStation 1. This file contains a specialized polygon rendering function using the PlayStation's GTE (Geometry Transformation Engine), focused on processing polygon lists with multiple vertices and generating optimized GPU commands.

## Table of Contents

1. [Polygon Rendering Module](#polygon-rendering-module)

---

## Polygon Rendering Module

This module contains functions related to rendering complex polygons using the PlayStation 1's GTE, including polygon list processing, vertex transformations, culling, and GPU command generation.

### FUN_80066ef8

**Lines:** 2-138  
**Type:** `void FUN_80066ef8(int param_1, int *param_2, undefined4 param_3, uint param_4)`

**Description:**  
Processes a linked list of polygons and generates GPU commands for rendering. This function is optimized to process multiple polygons in sequence, applying GTE transformations, performing culling, and generating type 5 commands (quadrilaterals) in the polygon sorter.

**Parameters:**
- `param_1`: Pointer to rendering context structure
  - Offset 0x68: Pointer to GPU command buffer (polygon sorter)
  - Offset 0x98: Scale parameter for depth calculation (ushort)
  - Offset 0x64 (100): Base address of texture/color table (int)
  - Offset 0x398: Culling inversion flag (char)
  - Offset 0x3b0: Flags mask for rendering (uint)
- `param_2`: Pointer to linked list of polygons
  - First integer: Number of vertices in current polygon
  - Following: Vertex data (encoded X, Y, Z coordinates)
  - Last integer: Pointer to next polygon (0 if end of list)
- `param_3`: Unused parameter (reserved)
- `param_4`: Reference Z coordinate for depth calculations

**Processing:**

1. **External Loop (Polygon List):**
   - Reads number of vertices of current polygon (`*param_2`)
   - Processes polygon until finding null pointer (0) indicating end of list

2. **Initial Vertex Transformation:**
   - Loads first 3 vertices into GTE using `gte_ldVXY0` and `gte_ldVZ0`
   - Applies perspective transformation using `gte_rtps_b()`
   - Extracts Z coordinates from vertices for depth calculations
   - Stores transformation flags (`gte_stFLAG()`)

3. **Internal Loop (Polygon Vertices):**
   - For each additional vertex (from 4th onwards):
     - Loads vertex into GTE
     - Applies perspective transformation
     - Stores screen coordinates (`gte_stSXY0()`, `gte_stSXY1()`, `gte_stSXY2()`)
     - Reads depth values using `read_sz_fifo4()`

4. **Depth Calculation:**
   - Determines closest or farthest vertex based on flag
   - Applies scale using parameter from `param_1 + 0x98`
   - Calculates offset in texture table based on depth
   - Limits maximum offset to 0x3ffc (4092)

5. **Polygon Culling:**
   - Performs back-face culling using `gte_nclip_b()`
   - Checks transformation flags for invalid polygons
   - Applies culling inversion if flag `param_1 + 0x398` is set
   - Uses bitwise operations to determine visibility

6. **GPU Command Generation:**
   - If polygon is visible:
     - Generates type 5 command (quadrilateral)
     - Configures rendering flags (`0x28000000`)
     - Stores vertex coordinates
     - Stores pointers to texture/color data
     - Writes command to buffer (`param_1 + 0x68`)
     - Increments buffer pointer by 0x18 bytes (24 bytes per command)

**Dependencies:**
- GTE functions:
  - `gte_ldVXY0`: Loads X, Y coordinates of vertex
  - `gte_ldVZ0`: Loads Z coordinate of vertex
  - `gte_rtps_b()`: Applies perspective transformation
  - `gte_stFLAG()`: Gets transformation flags
  - `gte_stSXY0()`, `gte_stSXY1()`, `gte_stSXY2()`: Stores screen coordinates
  - `gte_nclip_b()`: Calculates normal for culling
  - `gte_stMAC0()`: Gets calculation result
- `read_sz_fifo4()`: Reads depth values from GTE FIFO

**Data Structure:**

**Polygon List:**
```
[counter] [v0_x] [v0_y] [v0_z] [v1_x] [v1_y] [v1_z] [v2_x] [v2_y] [v2_z] [v3_x] [v3_y] [v3_z] ... [next_polygon]
```

**GPU Command Type 5 (24 bytes):**
```
Offset 0x00: Pointer to next command
Offset 0x04: Flags and type (0x28000000 | flags)
Offset 0x08: Vertex 1 coordinates
Offset 0x0c: Vertex 2 coordinates
Offset 0x10: Vertex 3 coordinates
Offset 0x14: Pointer to texture/color data
```

**Notes:**
- Function optimized to process linked lists of polygons
- Supports polygons with variable number of vertices (minimum 3)
- Uses careful memory alignment for GPU command writing
- Implements efficient culling using bitwise operations
- Calculates depth for sorting and texture selection
- Generates commands directly in format expected by PS1 GPU
- Uses externally managed command buffer (polygon sorter)

**Optimizations:**
- Processes multiple polygons in a single loop
- Reuses transformation calculations between vertices
- Uses bitwise operations for fast culling decisions
- Minimizes memory access through value caching

**Use Cases:**
- Rendering 3D objects with multiple polygons
- Processing complex polygon meshes
- Efficient GPU command generation for 3D scenes
- Rendering objects with variable number of faces

---

## Cross References

### Rendering Flow

1. High-level function (e.g., `FUN_80061798`) prepares vertex data
2. `FUN_80066ef8` processes polygon list
3. GPU commands are written to polygon sorter
4. PlayStation GPU renders polygons in specified order

### Module Dependencies

- **Rendering System:** Uses rendering context structure (`param_1`)
- **GTE:** Extensively uses Geometry Transformation Engine functions
- **Polygon Sorter:** Writes commands to externally managed buffer

---

## Final Notes

This file contains a highly optimized function for PlayStation 1 hardware, focused on efficient processing of polygon lists. The function demonstrates advanced techniques in:

- Efficient use of GTE for 3D transformations
- Manual memory management for GPU commands
- Loop optimization for batch processing
- Culling implementation using bitwise operations
- Careful data alignment for memory writing

The function is an essential part of Gran Turismo 2's 3D rendering pipeline, working together with other rendering functions to produce the game's final scene.
