# Documentation for File scus_944.88_part_017.c

## Overview

This document describes all functions present in the file `scus_944.88_part_017.c`, which contains decompiled code from Gran Turismo 2 for PlayStation 1. This file contains a single extremely complex function related to overlay0, which implements an advanced 3D rendering system with support for multiple polygon types and adaptive recursive subdivision.

## Table of Contents

1. [3D Rendering with Adaptive Recursive Subdivision Module (Overlay0)](#3d-rendering-with-adaptive-recursive-subdivision-module-overlay0)

---

## 3D Rendering with Adaptive Recursive Subdivision Module (Overlay0)

This module contains an extremely complex function that renders multiple types of 3D polygons using GTE with support for adaptive recursive subdivision based on visual quality and performance criteria.

### FUN_overlay0__800234f8

**Lines:** 2-2814  
**Type:** `void FUN_overlay0__800234f8(SVECTOR **param_1, uint param_2, uint param_3)`

**Description:**  
Extremely complex and extensive function (over 2800 lines) that implements an advanced 3D rendering system with support for up to 9 different polygon types and adaptive recursive subdivision. This function is an alternative/complement to function `FUN_overlay0__8002106c` and processes polygons using different rendering techniques based on visual quality criteria, camera distance, and screen size.

**Parameters:**
- `param_1`: Pointer to array of SVECTOR pointers containing polygon data to render
  - `param_1[0]`: Pointer to base vertices
  - `param_1[1]`: Pointer to triangles with solid colors (Gouraud shading)
  - `param_1[2]`: Pointer to triangles with textures
  - `param_1[3]`: Pointer to triangles with color gradients (3 vertices with different colors)
  - `param_1[4]`: Pointer to quadrilaterals with textures
  - `param_1[5]`: Pointer to triangles with textures and recursive subdivision
  - `param_1[6]`: Pointer to quadrilaterals with textures and recursive subdivision
  - `param_1[7]`: Pointer to triangles with gradients and recursive subdivision
  - `param_1[8]`: Pointer to quadrilaterals with gradients and recursive subdivision
  - `param_1[11]`: Flag indicating if there is data to process
- `param_2`: Control/flags parameter (used in transformation calculations)
- `param_3`: Control/flags parameter (used in transformation calculations)

**Features:**

1. **Initial Vertex Processing:**
   - Transforms vertices using GTE (`gte_ldv0`, `gte_rtps_b`)
   - Calculates clipping and visibility flags for each vertex
   - Stores results in temporary buffer (`local_2038`)

2. **Rendering Triangles with Solid Colors (Gouraud Shading):**
   - Processes triangles with colors interpolated between vertices
   - Applies lighting using `gte_dpcs()`
   - Checks clipping and culling before rendering
   - Generates GPU commands type `0x4000000` (polygon with solid colors)

3. **Rendering Triangles with Textures:**
   - Processes triangles with mapped textures
   - Applies texture transformations using GTE
   - Calculates UV coordinates and interpolates colors
   - Generates GPU commands type `0x5000000` (textured polygon)

4. **Rendering Triangles with Color Gradients:**
   - Processes triangles with 3 different colors per vertex
   - Uses `gte_dpct()` for color interpolation
   - Generates GPU commands type `0x6000000` (polygon with gradient)

5. **Rendering Quadrilaterals with Textures:**
   - Processes quadrilaterals (4 vertices) with textures
   - Divides quadrilaterals into triangles when necessary
   - Applies complex texture transformations
   - Generates GPU commands type `0x8000000` (textured quadrilateral)

6. **Rendering Triangles with Recursive Subdivision:**
   - Implements recursive subdivision based on:
     - Polygon size on screen
     - Distance from camera (Z depth)
     - Visual quality criteria
   - Subdivides triangles into 4 smaller sub-triangles (center + 3 edges)
   - Interpolates vertices, colors and texture coordinates
   - Applies recursion until stopping criterion is reached
   - Uses stack system to manage multiple subdivision levels

7. **Rendering Quadrilaterals with Recursive Subdivision:**
   - Similar to previous, but for quadrilaterals
   - Subdivides into multiple smaller triangles or quadrilaterals
   - Implements adaptive subdivision based on visual quality

8. **Rendering Polygons with Gradients and Subdivision:**
   - Combines color gradients with recursive subdivision
   - Interpolates colors during subdivision
   - Generates optimized GPU commands

9. **Adaptive Recursive Subdivision System:**
   - Uses memory area `0x1f800000` for temporary calculations
   - Manages subdivision stack using `DAT_1f8000e8`, `DAT_1f8000ec`, `DAT_1f8000f0`, `DAT_1f8000f4`
   - Implements multiple subdivision levels (up to 4-5 levels)
   - Subdivision criteria:
     - Polygon size on screen (using `DAT_1f800098`)
     - Distance from camera (Z depth)
     - Clipping flags (`0x20` indicates need for subdivision)
     - Recursion depth limit (`DAT_1f800390`)

10. **Rendering Optimizations:**
    - Frustum culling using `gte_nclip_b()`
    - Back-face culling using normal calculations
    - Z-sorting for depth ordering
    - Culling based on size and distance
    - Coordinate normalization to avoid overflow (using `gte_ldLZCS`, `gte_stLZCR`)

11. **GPU Buffer Management:**
    - Manages GPU command buffers dynamically
    - Uses `DAT_1f800068` as current buffer pointer
    - Checks buffer limits (`DAT_1f80006c`)
    - Generates correctly aligned GPU commands

**Dependencies:**
- GTE functions for transformations (`gte_ldv0`, `gte_rtps_b`, `gte_ldVXY0`, `gte_ldVZ0`, etc.)
- GTE functions for lighting (`gte_ldRGB`, `gte_dpcs`, `gte_dpct`, `gte_dpcs_b`, `gte_dpct_b`)
- GTE functions for clipping (`gte_nclip_b`, `gte_stMAC0`, `gte_stFLAG`)
- GTE functions for normalization (`gte_ldLZCS`, `gte_stLZCR`)
- `FUN_80081580`: Auxiliary rendering function (triangle subdivision)
- `FUN_8007bcd0`: Auxiliary rendering function (quadrilateral subdivision)
- `read_mt`: Reads transformation matrix data
- `read_sz_fifo3`: Reads Z depth data

**Data Structure:**

The function uses several temporary data structures:
- `local_2038`: Array of 8 shorts for vertex flags
- `auStack_2028`: Buffer of 8192 bytes for temporary data
- `DAT_1f8000e8` to `DAT_1f8000f4`: Pointers for subdivision stack
- `DAT_1f8000a8` to `DAT_1f8000d8`: Temporary vertices for subdivision
- `DAT_1f800388`, `DAT_1f80038c`, `DAT_1f800390`: Subdivision stack control

**GPU Command Types Generated:**

- `0x4000000`: Triangle with solid colors (Gouraud shading)
- `0x5000000`: Triangle with texture
- `0x6000000`: Triangle with color gradients
- `0x7000000`: Triangle with solid colors and subdivision
- `0x8000000`: Quadrilateral with texture and subdivision
- `0x9000000`: Triangle with gradient and subdivision
- `0xc000000`: Quadrilateral with gradient and subdivision

**Recursive Subdivision Algorithm:**

1. **Initialization:**
   - Copies original vertices to work area
   - Calculates clipping flags for each vertex
   - Checks if subdivision is necessary

2. **Subdivision Criteria:**
   - If polygon is too close to camera (small Z)
   - If polygon is too large on screen
   - If clipping flags indicate need (`0x20`)
   - If recursion depth has not exceeded limit

3. **Subdivision Process:**
   - For triangles: divides into 4 sub-triangles (center + 3 edges)
   - For quadrilaterals: divides into multiple smaller triangles or quadrilaterals
   - Interpolates vertices, colors and texture coordinates
   - Calculates new clipping flags for sub-polygons

4. **Recursion:**
   - Pushes data onto subdivision stack
   - Recursively calls for each sub-polygon
   - Pops when recursion completes

5. **Final Rendering:**
   - When stopping criterion is reached, renders polygon
   - Generates appropriate GPU commands
   - Updates rendering buffers

**Technical Notes:**

- This is one of the most complex functions in the game, with over 2800 lines
- Implements complete adaptive recursive subdivision system
- Uses GTE extensively for all transformations and calculations
- Optimizes rendering based on multiple criteria
- Manages memory dynamically for subdivision stack
- Supports up to 9 different polygon types simultaneously
- Implements multiple optimization levels (culling, clipping, sorting)

**Special Cases:**

- Very small polygons: renders directly without subdivision
- Very distant polygons: applies less subdivision to maintain performance
- Off-screen polygons: applies culling and doesn't render
- Back-facing polygons: applies back-face culling
- Coordinate overflow: normalizes using LZCS/LZCR

---

## Function Summary by Module

### 3D Rendering with Adaptive Recursive Subdivision Module (Overlay0)
- `FUN_overlay0__800234f8`: Advanced rendering of multiple polygon types with adaptive recursive subdivision

**Total documented functions:** 1

---

## Technical Observations

### Function Complexity
This function is extremely complex and represents one of the most advanced rendering systems in Gran Turismo 2. It implements:

- Complete adaptive recursive subdivision system
- Support for 9 different polygon types
- Multiple optimization levels
- Advanced memory and buffer management
- Deep integration with PlayStation GTE hardware

### GTE Usage
The function makes extensive use of GTE for:
- 3D transformations (rotation, translation, scale)
- Lighting and shading calculations
- Perspective projection
- Clipping and culling
- Color and texture interpolation
- Coordinate normalization

### Adaptive Recursive Subdivision
The subdivision system is adaptive and based on:
- Desired visual quality
- Available performance
- Polygon characteristics (size, distance, orientation)
- Configurable recursion limits

### Implemented Optimizations
- Frustum culling to eliminate off-screen polygons
- Back-face culling to eliminate non-visible polygons
- Z-sorting for depth ordering
- Culling based on size and distance
- Coordinate normalization to avoid overflow
- Optimized GPU command generation

### Memory Management
- Extensive use of memory area `0x1f800000` for temporary calculations
- Stack system to manage recursive subdivision
- GPU command buffers managed dynamically
- Efficient allocation of temporary structures
