---
alwaysApply: true
---
# Compilation Rules

## Overview

This project contains decompiled Gran Turismo 2 source code adapted to compile on Linux. The build system uses CMake with modern practices.

## Build System

### CMake Configuration

- **Minimum CMake Version**: 3.20
- **C Standard**: C11 (required)
- **Build Types**: Debug, Release, RelWithDebInfo, MinSizeRel

### Project Structure

```
GT2/_decompiled/
├── CMakeLists.txt              # Main build configuration
├── include/                    # Public headers
│   ├── gt2_types.h            # Common type definitions
│   ├── gt2_global_vars_clean.h # Global variable declarations
│   ├── disk_path.h            # Disk/ISO path resolution API
│   ├── gte_impl.h             # GTE implementation API (internal)
│   ├── gte_stubs.h            # GTE stub declarations (used by decompiled code)
│   └── ps1_stubs.h            # PS1 function stubs
├── src/
│   ├── decompiled/            # Decompiled source files
│   │   ├── scus_944.88_part_001.c through part_025.c
│   │   └── gt2_global_vars.c  # Global variable definitions
│   ├── external/              # DO NOT MODIFY (see "Rule: Never Alter src/external")
│   │   └── psyz/              # psyz + SDL3 (read-only)
│   ├── platform/              # Platform-specific code
│   │   ├── linux_main.c       # Linux entry point
│   │   ├── disk_path.c        # Disk/ISO path resolution for PSY-Z
│   │   ├── gte.c              # GTE software implementation (ported from Noah)
│   │   ├── gte_stubs.c        # GTE wrappers calling gte.c
│   │   └── ps1_stubs.c        # PS1 stub implementations
│   └── CMakeLists.txt         # Source build configuration
└── build/                     # Build directory (gitignored)
```

## Compilation Flags

### Debug Build
```cmake
-Wall -Wextra -Wpedantic -g -O0 -DDEBUG
```

### Release Build
```cmake
-Wall -Wextra -Wpedantic -O2 -DNDEBUG
```

### Additional Warnings
- `-Wunused-variable` - Warn about unused variables
- `-Wunused-function` - Warn about unused functions
- `-Wstrict-prototypes` - Require strict function prototypes
- `-Wmissing-prototypes` - Warn about missing prototypes

## Building

### Standard Build
```bash
mkdir -p build
cd build
cmake ..
make
```

### Build Types
```bash
cmake -DCMAKE_BUILD_TYPE=Debug ..
cmake -DCMAKE_BUILD_TYPE=Release ..
```

## Entry Point

The entry point is `main()` in `src/platform/linux_main.c`, which resolves the disk image path (CUE or ISO) for PSY-Z CD emulation, calls `Psyz_SetDiskPath()` when a path is found, then calls the original `start()` function from `scus_944.88_part_001.c:2185`.

## Running with a disk image (ISO / CUE)

CD emulation is provided by PSY-Z (`Psyz_SetDiskPath`). The game can use a disk image instead of a physical CD.

**Ways to specify the disk:**

1. **Command line**
   - `./gt2 -iso /path/to/image.iso`
   - `./gt2 -cue /path/to/image.cue`
   - `./gt2 -disk /path/to/image.cue`
   - `./gt2 /path/to/image.cue` (first positional argument)

2. **Next to the executable**  
   Place one of these in the same directory as the `gt2` binary (no arguments needed):
   - `scus_944.88.cue` / `scus_944.88.iso`
   - `gt2.cue` / `gt2.iso`
   - `disk.cue` / `disk.iso`

**Formats:**

- **CUE + BIN**: Use a `.cue` file that references your `.bin` (or other) data file. PSY-Z expects CUE format.
- **Raw ISO**: A single `.iso` file is supported; a temporary CUE is generated (single track MODE1/2352). For 2048-byte sector ISOs, use a CUE file that describes the layout instead.

**Example (image next to executable):**

```bash
cp /path/to/your/scus_944.88.cue /path/to/build/
cp /path/to/your/scus_944.88.bin /path/to/build/
./gt2
```

## Global Variables

Global variables follow this pattern:

1. **Declaration**: In `include/gt2_global_vars_clean.h` as `extern`
2. **Definition**: In `src/decompiled/gt2_global_vars.c`

When adding new global variables:
- Add `extern` declaration to the header
- Add definition to the .c file
- Initialize to zero or appropriate default value

## PS1 Stubs

PlayStation 1 specific functions are stubbed in `src/platform/ps1_stubs.c`:
- CD-ROM: Implemented by the **psyz** library when a disk path is set via `Psyz_SetDiskPath()` (see "Running with a disk image" above). The executable links to `psyz`, which provides CdInit, CdControl, etc.
- Pad/Controller functions (PadGetState, etc.)
- Memory card functions
- BIOS functions
- DCT/JPEG decoding functions

Stubs return default values (0, NULL, etc.) and can log calls when `ps1_stub_debug` is enabled.

## Common Issues and Solutions

### Missing Global Variables

**Error**: `'DAT_XXXXX' undeclared`

**Solution**: 
1. Add `extern undefined4 DAT_XXXXX;` to `include/gt2_global_vars_clean.h`
2. Add `undefined4 DAT_XXXXX = 0;` to `src/decompiled/gt2_global_vars.c`

### Missing Function Declarations

**Error**: `implicit declaration of function 'FUN_XXXXX'`

**Solution**:
1. Check if function exists in decompiled code
2. Add declaration to appropriate header file
3. If PS1-specific, add stub to `src/platform/ps1_stubs.c`

### Type Mismatches

**Error**: Type conflicts or size mismatches

**Solution**:
1. Check `include/gt2_types.h` for type definitions
2. Verify PS1 vs Linux type sizes (32-bit vs 64-bit)
3. Adjust types as needed for compatibility

### Register Variables

**Error**: `register0xXXXXX` undeclared

**Solution**: Replace with local stack variable or appropriate pointer:
```c
// Before:
DAT_80091168 = (undefined *)register0x00000074;

// After:
DAT_80091168 = (undefined *)&auStackX_0;  // Use local stack variable
```

## Code Style

- Use `.clang-format` for consistent formatting
- Follow existing code style in decompiled files
- Keep new code in English (comments and code)
- Document decisions in code comments

## Incremental Compilation

When fixing compilation errors:
1. Compile to see errors
2. Fix errors one at a time
3. Add missing declarations/definitions
4. Recompile and repeat

## Dependencies

- CMake 3.20+
- GCC or Clang with C11 support
- Standard C library
- **SDL3**: Used for window/OpenGL and audio; provided by the psyz submodule under `src/external/psyz`. The project always uses SDL3 (not SDL2).

## Rule: Never Alter `src/external`

**Do not modify any file or directory under `src/external`.**

### What it means

- No edits, patches, or deletions inside `src/external/`.
- No new files added under `src/external/`.
- Code in `src/external/` is treated as read-only third-party code.

### Why

- **Premise**: psyz is a working project from the start. The GT2 decompiled project must adapt to it; psyz is never modified.
- **External ownership**: Contents are submodules or vendored projects (e.g. psyz, SDL3) maintained elsewhere. Changes would be overwritten on update and are hard to track.
- **Reproducibility**: Keeping externals untouched makes builds and diffs predictable and avoids accidental breakage.
- **Upstream compatibility**: Easing future updates and reducing merge conflicts.

### What lives there

- **`src/external/psyz/`**: psyz library and its dependency SDL3 (e.g. `psyz/psyz/`, `psyz/external/SDL/`). Used for PS1 runtime support and SDL3 (video/audio/input).

### What to do instead

- **Use, don’t change**: Use headers, libraries, and CMake targets (e.g. `SDL3::SDL3`, `psyz`) from `src/external/` without editing their sources.
- **Project code**: Put all project-specific logic in `src/platform/`, `src/decompiled/`, `include/`, or other directories outside `src/external/`.
- **Build wiring**: Configure use of externals only in project CMake files that live *outside* `src/external/` (e.g. root `CMakeLists.txt`, `src/CMakeLists.txt`): include dirs, `target_link_libraries`, subdirectory order, options. Do not change CMake or source files inside `src/external/`.
- **If you need different behavior**: Prefer wrappers or glue code in `src/platform/` (or similar) that call into the external APIs, rather than patching the external tree.

### Summary

| Allowed | Not allowed |
|--------|-------------|
| Including headers from `src/external/` | Editing any file under `src/external/` |
| Linking to targets (e.g. `SDL3::SDL3`, `psyz`) | Adding or removing files under `src/external/` |
| Configuring build from *outside* `src/external/` | Patching or “fixing” code inside `src/external/` |

## Project Rules

- **Never alter anything inside `src/external`**: See the section “Rule: Never Alter `src/external`” above.
- **Adapt to psyz, never modify it**: psyz was working from the start. When build errors involve psyz/SDL3 (e.g. `LIBDECOR_WINDOW_STATE_SUSPENDED` undeclared), adjust the environment (install libdecor 0.3.0+, correct dependency versions) instead of patching `src/external/`.
- **Always use SDL3**: Use SDL3 for platform code (e.g. `#include <SDL3/SDL.h>`). Do not switch to SDL2.

## Notes

- This is decompiled code - variable names and types may be incorrect
- Many functions are stubbed and return default values
- Global variables are initialized to zero by default
- The code may have bugs from decompilation that need fixing
